sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t CrazyGoatStudio/apigator .
  - docker run -it --rm --name travis-eve CrazyGoatStudio/apigator 
  - docker ps -a
#  - docker run carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec rake test"

script:
#  - bundle exec rake test