sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t crazygoatstudio/apigator .
  - docker run -d -p 0.0.0.0:5000:5000 --name travis-eve crazygoatstudio/apigator
  - docker ps -a
  - sleep 15

script:
#  - bundle exec rake test
  - curl -i http://localhost:5000
  - curl -i http://localhost:5000/lists

after_script:
 - docker stop travis-eve
 - docker rm travis-eve
